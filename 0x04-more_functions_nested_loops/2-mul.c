#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: int to multiply
 * @b: int to multiply
 * Return: gives the result of both.
 */
int mul(int a, int b)
{
	return (a * b);
}
